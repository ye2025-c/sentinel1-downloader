requests>=2.28.0
tqdm>=4.64.0
ttkbootstrap>=1.10.0
tkintermapview>=1.29

# 可选：启用「NASA 下载后裁剪（瘦身）」需安装；缺失时该功能自动跳过、下载不受影响。
#   .nc（OMPS/S5P）裁剪需 netCDF4；.he5（OMI）裁剪需 h5py。
# numpy>=1.24
# netCDF4>=1.6
# h5py>=3.8
